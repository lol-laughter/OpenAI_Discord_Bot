# name: Keep Render Service Awake

# on:
#   schedule:
#     - cron: '*/5 * * * *' # 5分間隔で実行
#   workflow_dispatch:

# jobs:
#   keep-awake:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Keep Render Service Awake
#         run: |
#           echo "Pinging Render server..."
#           curl -v --retry 3 --retry-delay 5 "https://openai-discord-bot-3q6c.onrender.com"